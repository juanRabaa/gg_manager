angular.bootstrap("#gg-page-creator-panel", ['panelProductos']);
console.log("ready");
/*$(document).on("click", ".not-collapse, .button-name", function(e) {

    e.preventDefault();
    e.stopPropagation();
    $('.collapsible').collapsible('close');
    console.log(1);
});
*/
